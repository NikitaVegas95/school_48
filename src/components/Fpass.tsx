import React, { FC } from 'react';
import '../styles/main.scss'

const Fpass: FC = () => (
  <div className={'reset-wrapper'}>
    <a className={"reset-pass"}>Забыли пароль?</a>
  </div>
);

export default Fpass